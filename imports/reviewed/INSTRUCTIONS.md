# Reviewed Imports

Move external import notes here after review is complete.

Reviewed does not mean active. Active practices must still live under `practices/` and be indexed in `indexes/practice_index.yaml`.

